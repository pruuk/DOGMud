# Playtest Harness Adoption — Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace DOGMud's file-IPC AI-testing stack with the GoMud playtest harness's `mudagent` adapter + the vendored `playtest` server module (beacons, safe mode, death protection, sandbox) + a DOGMud `/playtest` driver reading a self-contained `tools/playtest/` overlay; retire the old stack.

**Architecture:** Approach A — the harness repo (`~/workspace/gomud-playtest-harness`, env `GOMUD_HARNESS_DIR`, default `../gomud-playtest-harness`) stays engine-agnostic; DOGMud owns a thin content overlay + driver and vendors only the server module. The module's engine prerequisites (AI port `ConnAI`, `IsAI`, gmcp `SendGMCPEvent` export, events/rooms) are **already present in DOGMud** (verified) — the only code adaptations are the beacon payload (DOGMud's pools are Health/Stamina/Conviction, not Mana) and death-protection (DOGMud has no `Character.ExtraLives`).

**Tech Stack:** Go (server module, in DOGMud's main module — modules use `github.com/GoMudEngine/GoMud/...` import paths and have **no per-module go.mod**; `modules/all-modules.go` is `go generate`-d). YAML/Markdown (overlay). The prebuilt `mudagent` binary + the harness driver are reference sources at `$GOMUD_HARNESS_DIR`.

**Spec:** `docs/superpowers/specs/completed/2026-06-08-playtest-harness-adoption-design.md`
**Reference (harness) docs:** `module/playtest/*.go` (source to vendor), `docs/plans/2026-06-05-playtest-module.md`, `docs/plans/2026-06-05-phase2-beacons.md`, `docs/plans/2026-06-05-framework-content.md`, `.claude/commands/playtest.md` (driver to adapt), `framework/{engine-profile.yaml,targets.yaml,personalities,goals/SCHEMA.md}` — all under `$GOMUD_HARNESS_DIR`.

---

## File Structure

- `modules/playtest/*.go` + `modules/playtest/files/**` — **new**, vendored from the harness (no `go.mod`), with two adaptations (beacon fields, death protection).
- `modules/all-modules.go` — **regenerated** (`go generate`) to register `playtest`.
- `tools/playtest/engine-profile.yaml`, `targets.yaml`, `personalities/*.md`, `goals/*.yaml`, `reports/.gitkeep` — **new** DOGMud overlay.
- `.claude/commands/playtest.md` — **new** DOGMud driver (adapted from the harness driver).
- `tools/_archive/testing-pre-harness/` + `README.md` — **new** archive; old stack `git mv`-d in.
- `.gitignore` — **edit** (ignore `tools/playtest/.run/` + `tools/playtest/reports/*`).

Build order: server module first (the foundation), then overlay, driver, archive, acceptance.

---

## Task 1: Vendor the playtest server module (source + registration)

Copy the module source into DOGMud and get it compiling/registered, *before* the DOGMud-specific adaptations (Tasks 2–3) so each adaptation is an isolated diff.

**Files:**
- Create: `modules/playtest/{playtest.go,config.go,commands.go,beacons.go,safemode.go,config_test.go,beacons_test.go,safemode_test.go}` and `modules/playtest/files/**` (copied from `$GOMUD_HARNESS_DIR/module/playtest/`, **excluding `go.mod`**)
- Modify (regenerate): `modules/all-modules.go`

- [ ] **Step 1: Copy the module source (no go.mod)**

```bash
HARNESS="${GOMUD_HARNESS_DIR:-../gomud-playtest-harness}"
mkdir -p modules/playtest
cp -r "$HARNESS"/module/playtest/. modules/playtest/
rm -f modules/playtest/go.mod   # DOGMud modules have NO per-module go.mod
```
The harness `module/playtest/go.mod` itself documents "Do NOT copy this go.mod into a GoMud `modules/` directory." The package path stays `github.com/GoMudEngine/GoMud/modules/playtest`, which matches DOGMud's module path.

- [ ] **Step 2: Register the module via go generate**

`modules/all-modules.go` is generated (`// Code generated by go:generate; DO NOT EDIT.`). Regenerate so it picks up the new dir:
```bash
go generate ./...
grep playtest modules/all-modules.go
```
Expected: an `_ "github.com/GoMudEngine/GoMud/modules/playtest"` import line now present. (If `go generate` does not auto-discover module dirs, locate the generator directive — `grep -rn "go:generate" *.go modules/*.go` — and run it; do **not** hand-edit the generated file.)

- [ ] **Step 3: Build — expect failures at the two known adaptation points**

Run: `go build ./...`
Expected: compile errors in `modules/playtest/beacons.go` (`u.Character.Mana`/`ManaMax` undefined — DOGMud uses `Stamina`) and `modules/playtest/safemode.go` (`u.Character.ExtraLives` undefined). These are fixed in Tasks 2–3. If there are OTHER errors (e.g. `events.NewRound.RoundNumber`, `plug.Web.AdminPage` signature, `events.PlayerSpawn.ConnectionId`), note them — they indicate further divergence to handle in this task before proceeding.

- [ ] **Step 4: Resolve any divergence beyond the two known points**

For each non-beacon/non-deathprotection compile error: verify the DOGMud symbol (`grep`/codegraph) and adapt the vendored source minimally to DOGMud's actual API (e.g. correct event field name, `plug.Web.AdminPage` arg count). Do NOT change module behavior — only names/signatures. If `plug.Web.AdminPage` differs, match DOGMud's signature (see `modules/leaderboards` or `modules/auctions` for a working `plug.Web.AdminPage` call). Leave the beacon + death-protection errors for Tasks 2–3.

- [ ] **Step 5: Commit (compiles up to the 2 intentional gaps, or fully if Step 4 cleared them)**

If the only remaining errors are the beacon/death-protection ones, commit the vendored source as-is is NOT possible (won't build). Instead, do Tasks 2–3 first, then commit all three together. Mark this task done when Steps 1–4 are complete and the ONLY remaining build errors are the two known adaptations.

---

## Task 2: Adapt the beacon payload to DOGMud's three pools

DOGMud's `Character` has `Health`/`HealthMax`, `Stamina`/`StaminaMax`, and a third pool `Conviction`/`ConvictionMax` (all `*Max` are `stats.StatInfo`, read `.Value`). The stock beacon carries only HP/SP and reads `Mana`. Add CP and fix the SP source.

**Files:**
- Modify: `modules/playtest/beacons.go`
- Modify: `modules/playtest/beacons_test.go`

- [ ] **Step 1: Update the beacon test first (TDD)**

In `modules/playtest/beacons_test.go`, extend the payload assertion to expect the three-pool shape. The existing test builds a beacon and checks fields; add `cp`/`cp_max` expectations and change the SP source field. Concretely, the payload under test must serialize to keys `round, hp, hp_max, sp, sp_max, cp, cp_max, room_id`. Update the fixture `Character` to set `Stamina`/`StaminaMax`/`Conviction`/`ConvictionMax` and assert `sp`/`sp_max` come from Stamina and `cp`/`cp_max` from Conviction. (Read the existing test to match its construction style.)

- [ ] **Step 2: Run it — verify it fails**

Run: `go test ./modules/playtest/ -run TestBeacon -v`
Expected: FAIL (compile error or missing cp fields).

- [ ] **Step 3: Adapt `beaconPayload` and `onNewRound`**

In `modules/playtest/beacons.go`, change the struct to:
```go
type beaconPayload struct {
	Round  uint64 `json:"round"`
	HP     int    `json:"hp"`
	HPMax  int    `json:"hp_max"`
	SP     int    `json:"sp"`
	SPMax  int    `json:"sp_max"`
	CP     int    `json:"cp"`
	CPMax  int    `json:"cp_max"`
	RoomID int    `json:"room_id"`
}
```
and in `onNewRound`, the `m.sendGMCP(...)` payload to:
```go
m.sendGMCP(u.UserId, "Playtest.Round", beaconPayload{
	Round:  evt.RoundNumber,
	HP:     u.Character.Health,
	HPMax:  u.Character.HealthMax.Value,
	SP:     u.Character.Stamina,
	SPMax:  u.Character.StaminaMax.Value,
	CP:     u.Character.Conviction,
	CPMax:  u.Character.ConvictionMax.Value,
	RoomID: u.Character.RoomId,
})
```
(Verify `evt.RoundNumber` is the correct field on DOGMud's `events.NewRound` — `grep -n "RoundNumber" internal/events/*.go`; if DOGMud names it differently, use that name.)

- [ ] **Step 4: Run the test — verify it passes**

Run: `go test ./modules/playtest/ -run TestBeacon -v`
Expected: PASS.

---

## Task 3: Map death protection to DOGMud's death model

Stock `applyDeathProtection` sets `u.Character.ExtraLives = 999`; DOGMud's `Character` has **no `ExtraLives`** (justice/jail death model, no permadeath/bleedout). Determine DOGMud's actual behavior and adapt.

**Files:**
- Modify: `modules/playtest/safemode.go`
- Modify: `modules/playtest/safemode_test.go`

- [ ] **Step 1: Determine DOGMud's death/lives model**

Run: `grep -rniE "ExtraLives|permadeath|PermaDeath|extra_lives|respawn|Respawn" internal/characters/ internal/configs/ internal/users/ | head -20`
Decide which case holds:
- **(a) No permadeath / no lives concept** (expected — DOGMud uses justice/jail): death-protection is a **no-op**. Implement `applyDeathProtection` to return early with a comment that DOGMud has no permadeath, so AI testers already cannot permanently die; keep the `onPlayerSpawn` listener registration harmless (or skip registering it).
- **(b) There IS a lives/permadeath field** under a different name: set it high (mirror the stock intent) using DOGMud's field.
Record which case you found in the commit message.

- [ ] **Step 2: Update the death-protection test (TDD)**

In `modules/playtest/safemode_test.go`, adjust the death-protection test to DOGMud's model:
- case (a): assert `applyDeathProtection` is safe to call and does not panic / does not require an `ExtraLives` field (e.g. it returns without touching the character, or sets the real field). Remove the assertion on `ExtraLives` and replace with an assertion appropriate to the chosen behavior.
- case (b): assert the real field is set high.

- [ ] **Step 2b: Run it — verify it fails**

Run: `go test ./modules/playtest/ -run TestDeath -v` (or the actual test name in the file)
Expected: FAIL (still references `ExtraLives`).

- [ ] **Step 3: Implement the adaptation**

Edit `applyDeathProtection` in `modules/playtest/safemode.go` per the case from Step 1. For case (a):
```go
// applyDeathProtection is a no-op on DOGMud: the engine has no permadeath /
// extra-lives mechanic (death routes through the justice/jail system, and
// bleedout/downed were removed), so an AI tester cannot permanently die. Kept
// for interface parity with the stock module + future engines.
func (m *PlaytestModule) applyDeathProtection(u *users.UserRecord) {
	_ = u
}
```
(Leave `onPlayerSpawn` + its `registerSafeMode` registration in place — it simply calls the now-no-op protector; or, if cleaner, gate the `events.PlayerSpawn` registration behind nothing changes in behavior. Keep the diff minimal.)

- [ ] **Step 4: Run module tests — verify all pass**

Run: `go test ./modules/playtest/ -v`
Expected: PASS (config, beacons, safemode).

- [ ] **Step 5: Build + commit Tasks 1–3 together**

```bash
go build ./...
git add modules/playtest modules/all-modules.go
git commit -F - <<'MSG'
feat(test): vendor the playtest server module, adapted for DOGMud

Vendor module/playtest from the GoMud playtest harness into modules/playtest
(no per-module go.mod; registered via go generate). DOGMud already has the
engine prerequisites (ConnAI, IsAI, AI listener, gmcp SendGMCPEvent export),
so only two adaptations were needed: the beacon now carries DOGMud's three
pools (Health/Stamina/Conviction) instead of HP/Mana, and death-protection is
<no-op | mapped to DOGMud's <field>> since DOGMud has no Character.ExtraLives.

Co-Authored-By: <playtest module author> 
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
MSG
```
(Fill the module author from `git -C "$GOMUD_HARNESS_DIR" log --format='%an <%ae>' -1 -- module/playtest` — credit the original author per project convention.)

- [ ] **Step 6: Boot smoke — module loads, beacons available**

Nuke instance saves per SOP, boot the server (AI port already on at `AIPort: 55555`), and confirm clean start with NO `playtest` warning that gmcp `SendGMCPEvent` is missing (that warning would mean the gmcp export isn't found). Look for the module's load and `Server Ready`.

---

## Task 4: DOGMud playtest overlay — engine profile + targets + personalities

**Files:**
- Create: `tools/playtest/engine-profile.yaml`, `tools/playtest/targets.yaml`, `tools/playtest/personalities/{bug-finder,feature-tester,feel-tester}.md`, `tools/playtest/reports/.gitkeep`
- Modify: `.gitignore`

- [ ] **Step 1: Write `tools/playtest/engine-profile.yaml`**

```yaml
# Engine profile — the ONE place DOGMud-specific facts live, so personalities
# and the driver stay engine-agnostic. The agent reads this for command names,
# world orientation, and mechanics.
engine: DOGMud

commands:
  look: look
  movement: [north, south, east, west, up, down]
  inventory: inventory
  status: status
  examine: look <target>
  get: get <item>
  drop: drop <item>
  attack: attack <target>
  flee: flee
  say: say <message>
  recover: ["cast <heal-spell> me", "drink <potion>"]   # DOGMud heals via spells/potions
  help: help
  map: map
  consider: consider <target>            # gauge relative strength before a fight
  special_moves: [kick, bash, taunt]     # stamina/positional specials
  cast: cast <spell> <target>

# Run immediately after login — force ASCII so box-drawing/emoji don't corrupt output.
setup_commands: ["set charset"]

world:
  starting_area: >-
    New DOGMud characters begin in Sanctum Basin (the newbie hub, around the
    Town Square). Rooms describe their exits and any visible NPCs.
  onboarding: >-
    If you connect with no existing character, create one at the login prompt:
    enter "new", then a username, password, password again, email (optional),
    screen-reader y/n, and confirm. New characters may begin pre-tutorial — take
    the tutorial or choose to start playing to become a full character.
  orientation: >-
    Movement is compass-based (north/south/east/west/up/down). `look` shows the
    room, exits, and who/what is here; `look <noun>` examines a target. `map`
    shows the local map.

mechanics:
  health: >-
    Characters have THREE pools: Health (HP), Stamina (SP, physical energy), and
    Conviction (CP, mental/spiritual energy). The per-round beacon reports all
    three (hp/sp/cp + max). Pools regen over time; heal with spells (`cast`) or
    potions (`drink`).
  death: >-
    DOGMud has NO permadeath and no bleedout/downed state. Lethal outcomes route
    through a justice/jail system (arrest, bounties); a defeated character is not
    permanently lost. Avoid attacking live players or important NPCs anyway.
  combat: >-
    `attack <target>` starts combat; `flee` escapes. Combat resolves per round on
    a ~4-second timer. Specials: kick/bash/taunt; `cast <spell>` for magic.
  progression: >-
    There are NO levels or XP. The six stats (Strength, Dexterity, Perception,
    Vitality, Willpower, Charisma, centered at 100) and nine skills improve by
    USE, probabilistically. Don't look for an XP bar or level-ups.

notes:
  - The AI port already delivers ANSI-stripped output — do NOT file display
    artifacts as bugs.
  - NPC/target names must be exact keywords from the room description; `look` to
    re-read if targeting fails.
```

- [ ] **Step 2: Write `tools/playtest/targets.yaml`**

```yaml
# Connection targets. The DOGMud /playtest driver reads host/port/user/password.
local:
  host: localhost
  port: 55555
  user: smoketester
  password: smoke123test
prod:
  host: dogmud.org
  port: 55555
  user: aitester
  password: testpass123
```

- [ ] **Step 3: Copy the three personalities (DOGMud-flavored)**

```bash
HARNESS="${GOMUD_HARNESS_DIR:-../gomud-playtest-harness}"
mkdir -p tools/playtest/personalities
cp "$HARNESS"/framework/personalities/{bug-finder,feature-tester,feel-tester}.md tools/playtest/personalities/
```
Then add a short DOGMud note to the bottom of each (do NOT rewrite them): three-pool survival (watch HP **and** SP/CP via the beacon), death is non-permanent (justice/jail), and `set charset` ASCII is already handled by the driver. Keep edits to a few lines per file.

- [ ] **Step 4: Reports dir + .gitignore**

```bash
mkdir -p tools/playtest/reports && touch tools/playtest/reports/.gitkeep
```
Append to `.gitignore`:
```
# Playtest harness overlay — transient run state + generated reports
tools/playtest/.run/
tools/playtest/reports/*
!tools/playtest/reports/.gitkeep
```

- [ ] **Step 5: Commit**

```bash
git add tools/playtest/engine-profile.yaml tools/playtest/targets.yaml tools/playtest/personalities tools/playtest/reports/.gitkeep .gitignore
git commit -m "feat(test): DOGMud playtest overlay — engine profile, targets, personalities"
```

---

## Task 5: Migrate a curated subset of goals to the harness schema

Migrate the currently-useful goals (not the ~50 stale chunk-smokes) to the harness `{id, do, verify}` schema. Schema: see `$GOMUD_HARNESS_DIR/framework/goals/SCHEMA.md` — `name` + `summary`, `goals[].{id, do, verify}`, optional `notes[]`, `pass_criteria[]`.

**Files:**
- Create: `tools/playtest/goals/{shop-economy,cooking-supply-chain,crime-justice,messaging,stillwater-feel}.yaml`

- [ ] **Step 1: Pick + migrate the subset**

Migrate these five from `tools/testing/goals/` (representative across systems), converting `description`→`name`/`summary`, each `goals[].text`→`do` (the attempt) + `verify` (a success check split out of the text), and `exit_criteria`→`pass_criteria`:
- `vendor-economy-polish-tutorial-regression.yaml` → `shop-economy.yaml`
- `cooking-supply-chain.yaml` → `cooking-supply-chain.yaml`
- `crime-system-thornwall-smoke.yaml` → `crime-justice.yaml`
- `messaging-framework-smoke.yaml` → `messaging.yaml`
- `stillwater-town-flavor.yaml` → `stillwater-feel.yaml`

For each, prefer **beacon/gmcp** checks in `verify` over text scraping where natural (e.g. "gold decreased / beacon `hp` rose"). Use `$GOMUD_HARNESS_DIR/framework/goals/examples/feature-tester-shop-economy.yaml` as the shape reference.

- [ ] **Step 2: Validate YAML**

Run: `python -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('tools/playtest/goals/*.yaml')]; print('ok')"`
Expected: `ok`.

- [ ] **Step 3: Commit**

```bash
git add tools/playtest/goals
git commit -m "feat(test): migrate a curated goal subset to the harness schema"
```

---

## Task 6: DOGMud `/playtest` driver

Adapt the harness reference driver to read the DOGMud overlay and locate the harness binary.

**Files:**
- Create: `.claude/commands/playtest.md`

- [ ] **Step 1: Copy the harness driver as the base**

```bash
cp "${GOMUD_HARNESS_DIR:-../gomud-playtest-harness}/.claude/commands/playtest.md" .claude/commands/playtest.md
```

- [ ] **Step 2: Apply the DOGMud adaptations**

Edit `.claude/commands/playtest.md` with exactly these changes (the rest of the driver logic — the JSON event loop, login/char-creation, goal judging, report writing — stays):
1. **Config paths:** every `framework/targets.yaml` → `tools/playtest/targets.yaml`; `framework/personalities/<p>.md` → `tools/playtest/personalities/<p>.md`; `framework/engine-profile.yaml` → `tools/playtest/engine-profile.yaml`; `framework/goals/<f>` → `tools/playtest/goals/<f>`.
2. **Locate the harness binary:** add, before the adapter-start step, a resolution block:
   > Resolve the harness: `HARNESS="${GOMUD_HARNESS_DIR:-../gomud-playtest-harness}"`. If `$HARNESS/mudagent.exe` (Windows) or `$HARNESS/mudagent` exists, use it; else run `go run ./cmd/mudagent` from `$HARNESS`. If `$HARNESS` doesn't exist, STOP with: "playtest harness not found at $HARNESS — set GOMUD_HARNESS_DIR or clone GoMudEngine/GoMud-Module-Playtest-Harness."
3. **Run dir:** bridge stdio under `tools/playtest/.run/` (`commands.txt` / `events.jsonl`), not `.playtest/`. The `mudagent` invocation uses `--target <host>:<port>` and, only if the target has them, `--user/--password` from `tools/playtest/targets.yaml`.
4. **Reports:** write to `tools/playtest/reports/YYYY-MM-DD-<target>-<personality>-<goals-or-session>.md`.
5. **Beacon pacing:** keep the harness's beacon-based pacing (the vendored module now emits `Playtest.Round` with hp/sp/cp/room) — note CP is available in the beacon for DOGMud.
6. **Usage line:** `/playtest <local|prod> <personality> [goals-file]`.

- [ ] **Step 3: Sanity-check the driver references**

Run: `grep -nE "framework/|\.playtest/" .claude/commands/playtest.md`
Expected: **no output** (all `framework/` and `.playtest/` references replaced with `tools/playtest/` and `tools/playtest/.run/`).

- [ ] **Step 4: Commit**

```bash
git add .claude/commands/playtest.md
git commit -m "feat(test): DOGMud /playtest driver (adapted from harness reference)"
```

---

## Task 7: Retire the old testing stack into an archive

**Files:**
- Move (`git mv`): `tools/mud_bridge.py`, `tools/ai_player.py`, `.claude/commands/test-mud.md`, `tools/testing/**`
- Create: `tools/_archive/testing-pre-harness/README.md`

- [ ] **Step 1: Move the superseded files (history preserved)**

```bash
mkdir -p tools/_archive/testing-pre-harness
git mv tools/mud_bridge.py tools/_archive/testing-pre-harness/
git mv tools/ai_player.py  tools/_archive/testing-pre-harness/
git mv .claude/commands/test-mud.md tools/_archive/testing-pre-harness/test-mud.md
git mv tools/testing tools/_archive/testing-pre-harness/testing
```

- [ ] **Step 2: Archive README**

Create `tools/_archive/testing-pre-harness/README.md`:
```markdown
# Archived: pre-harness AI testing (2026-06-08)

Superseded by the GoMud playtest harness adoption (Phase 1):
- `mud_bridge.py` / `test-mud.md` → the `mudagent` adapter + `/playtest` driver
  (`.claude/commands/playtest.md`) + the `tools/playtest/` overlay.
- `ai_player.py` (standalone Anthropic-API autonomous player) → retired; the
  harness's bring-your-own-agent model replaces it.
- `testing/` (roles, goals, reports, audits) → roles became
  `tools/playtest/personalities/`; a curated goal subset was migrated to
  `tools/playtest/goals/`; the rest is kept here for historical reference.

Kept for history only. Do not wire back up.
```

- [ ] **Step 3: Confirm the old command no longer resolves + nothing references the moved files**

Run: `test ! -f .claude/commands/test-mud.md && echo "test-mud removed"` and `grep -rniE "mud_bridge|ai_player|tools/testing/" --include=*.md --include=*.py . | grep -v tools/_archive | head`
Expected: `test-mud removed`; the grep shows no live references (archive-internal references are fine).

- [ ] **Step 4: Commit**

```bash
git add -A tools/_archive tools/mud_bridge.py tools/ai_player.py .claude/commands/test-mud.md tools/testing
git commit -m "chore(test): archive pre-harness AI testing stack (replaced by /playtest)"
```
(If `git add -A` is too broad for the dirty tree, stage the specific moved paths + the new archive README explicitly.)

---

## Task 8: Acceptance — run a real DOGMud playtest

**Files:** none (verification)

- [ ] **Step 1: Full build + module tests**

Run: `go build ./... && go test ./modules/playtest/ -v`
Expected: build exit 0; module tests pass.

- [ ] **Step 2: Boot the server**

Nuke instance saves per SOP; boot (`go run .`); confirm `Server Ready`, the `playtest` module loaded, and no missing-`SendGMCPEvent` warning. (Ensure no other server holds port 80/55555 first.)

- [ ] **Step 3: Single-agent feel run**

From the DOGMud repo root, run `/playtest local feel-tester`. Confirm: adapter connects; `status: logged_in` (or a clean character-creation walkthrough); `set charset` ran; **`beacon` events arrive each round with `hp/sp/cp/room_id`**; several rounds of command→`output`/`gmcp` play; a report lands in `tools/playtest/reports/`.

- [ ] **Step 4: Goal-driven feature run**

Run `/playtest local feature-tester shop-economy.yaml`. Confirm goal-by-goal PASS/FAIL judging in the report, using gold/inventory/beacon state where possible.

- [ ] **Step 5: Record results**

If all pass, Phase 1 is done. If a specific item misbehaves, it's a config/overlay tweak (engine-profile, goal `verify`, or a module adaptation) — fix and re-run; logic changes are out of scope here.

---

## Self-Review notes (addressed)

- **Spec coverage:** server module vendored + adapted (T1–3, beacons + death-model per spec §1B); overlay engine-profile/targets/personalities (T4) + migrated goals (T5); `/playtest` driver (T6); old-stack retirement + `ai_player.py` archived (T7); acceptance incl. beacons + a goal run (T8). All Phase-1 spec items covered. Phase 2 (scenarios/`ptorch`) intentionally excluded — separate plan.
- **Verified-fact alignment:** beacon reads `Health/Stamina/Conviction` (+`.Value` on the `*Max` StatInfo); death-protection handles the absent `ExtraLives`; module needs no engine changes (ConnAI/IsAI/SendGMCPEvent present). The `evt.RoundNumber` and `plug.Web.AdminPage` shapes are flagged as verify-then-match in T1/T2.
- **Placeholder scan:** the `<heal-spell>`/`<potion>` tokens in engine-profile are intentional agent-facing guidance (the agent fills the real spell/potion from what it has), not plan placeholders. Commit-message `<playtest module author>` is resolved by the documented git command in T3 Step 5.
- **Type consistency:** `beaconPayload` fields/json tags are identical across T2 struct + test; the `tools/playtest/` paths are consistent across T4/T6; module package path `github.com/GoMudEngine/GoMud/modules/playtest` matches DOGMud's module path + `all-modules.go`.
