<ansi fg="black-bold">.:</ansi> <ansi fg="magenta">Help for </ansi><ansi fg="command">set</ansi> command

The <ansi fg="command">set</ansi> command configures your account. 

There are a handful of settings you can adjust with this command, including:

<ansi fg="yellow">Usage: </ansi>

  <ansi fg="command">set =# [command]</ansi> - e.g. <ansi fg="command">set =1 "say hi everyone"</ansi>
  Sets a macro # (0-10) to a command of your choice. You can then use this macro
  as a shortcut to quickly issue to a command as follows: <ansi fg="command">=1</ansi>
  If your terminal program supports it, prettying a corresponding F-Key
  Type <ansi fg="command">help macros</ansi> for more information about macros.

  <ansi fg="command">set description [description]</ansi> - e.g. <ansi fg="command">set description "A fearsome warrior"</ansi> 
  This sets the description players see when they look at your character. You
  can also look at yourself to see your current description.

  <ansi fg="command">set auction</ansi>
  This toggles the auction system on or off. If off, you will not receive
  notifications about system-wide auctions.

  <ansi fg="command">set shortadjectives</ansi>
  This toggles short adjective displayes on or off. See <ansi fg="command">help adjectives</ansi>

  <ansi fg="command">set prompt [prompt text]</ansi>
  This sets a custom prompt (See <ansi fg="command">help set-prompt</ansi>)
  If you ommit the [prompt text], your current prompt will be displayed.

  <ansi fg="command">set tinymap</ansi>
  This toggles the automatic tinymap on or off. It shows when looking at rooms.

  <ansi fg="command">set fprompt</ansi>
  Configure your fight prompt. Toggle elements on or off:
  <ansi fg="command">set fprompt bars</ansi>  <ansi fg="command">set fprompt target</ansi>  <ansi fg="command">set fprompt tank</ansi>
  Type <ansi fg="command">set fprompt</ansi> alone to see all toggle options.

  <ansi fg="command">set wimpy</ansi>
  Set your wimpy threshold (See <ansi fg="command">help set-wimpy</ansi>)

  <ansi fg="command">set submission <mercy|subdue|cripple|lethal></ansi>
  Sets what happens when you lock a submission on an opponent.
  Type <ansi fg="command">set submission</ansi> alone to see your current policy.
  See <ansi fg="command">help submission</ansi>.

  <ansi fg="command">set surrender <never|always|auto-tap-below N></ansi>
  Sets whether you tap out when caught in a submission.
  Type <ansi fg="command">set surrender</ansi> alone to see your current policy.
  See <ansi fg="command">help surrender</ansi>.

  <ansi fg="command">set charset</ansi>
  Toggles between UTF-8 and ASCII display modes. If your client
  shows garbled characters in tables and borders, switch to ASCII.

  <ansi fg="command">set combatverbosity <full|medium|light></ansi>
  Controls how much combat text you see. See <ansi fg="command">help combatverbosity</ansi>.

<ansi fg="magenta-bold">See also:</ansi> <ansi fg="command">help alias</ansi>, <ansi fg="command">help macros</ansi>,
  <ansi fg="command">help set-prompt</ansi>, <ansi fg="command">help submission</ansi>,
  <ansi fg="command">help surrender</ansi>
